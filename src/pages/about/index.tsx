import React, { ReactElement } from 'react'
import Layout from '../../components/layout'

const About = (props): ReactElement => {
  console.log(props)
  return (
    <Layout>
      <h1>Arsam</h1>
    </Layout>
  )
}

export default About
